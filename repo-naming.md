# Repo 命名规范（强制遵守）

## 核心原则

命名必须包含「**设备标识** + 研究方向/任务标识」（强制维度），可选补充「项目阶段」（如临时实验可加 `tmp-` 前缀）。

## 统一格式（2 种推荐，二选一，全实验室保持一致）

### 格式 1（推荐：设备优先，适配硬件驱动型项目）

`[设备品牌-型号]-[研究方向/任务]`
示例：

-   宇树 G1 强化学习：`unitreeG1-reinforcement-learning`
-   无人机视觉导航：`drone-visual-navigation`

### 格式 2（推荐：方向优先，适配算法驱动型项目）

`[研究方向]-[设备品牌-型号]`
示例：

-   具身智能-宇树 G1：`embodiedAI-unitreeG1`
-   强化学习-大疆无人机：`rl-drone`

## 命名要求（新生必看）

1. 用 `-` 分隔单词，禁止空格/特殊字符；
2. 设备标识必须精准到型号（如 `unitreeG1` 而非 `unitree`）；
3. 研究方向用英文通用缩写（如强化学习可简写 `rl`，具身智能可简写 `ei`）。
