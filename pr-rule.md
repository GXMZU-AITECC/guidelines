# PR 规范

提 PR 是日常协作的基本操作，下面列了一些约定，让整个过程更顺畅。

## 目标分支

PR 统一走 **`develop`**，`main` 只用来发版。

## PR 标题

标题格式统一为 `<type>: <描述>`。type 只有两个：

| type   | 什么时候用     |
| ------ | -------------- |
| `feat` | 加了新功能     |
| `fix`  | 修了个 Bug     |

举两个例子：

- `feat: 新增科学计算器模式`
- `fix: 修复 BIN 模式下负数转换错误`

## PR 描述

每个 PR 的描述写清楚三件事：

1. **目的** — 这个 PR 解决了什么问题、完成了什么效果
2. **改动** — 改了什么文件、做了什么（列表形式，一目了然）
3. **测试** — 改完之后测了什么、效果如何（也是列表形式）

模板：

```markdown
## 目的

...

## 改动

- ...

## 测试

- ...
```

## 提 PR 前检查

- 没有残留的调试代码、多余注释或文件
- 没删掉不该删的原有代码或注释
- 新功能或修 Bug 不影响已有功能
- 没有硬编码的临时数值
- 自己测过，没有新增报错
