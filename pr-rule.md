# C · PR 规范

## C1 目标分支（强制）

PR 统一走 **`develop`**；`main` 只用来发版。

## C2 PR 标题（强制）

格式统一为 `<type>: <描述>`，type 只有两个：

| type   | 什么时候用 |
| ------ | ---------- |
| `feat` | 加了新功能 |
| `fix`  | 修了 Bug   |

示例：

- `feat: 新增科学计算器模式`
- `fix: 修复 BIN 模式下负数转换错误`

## C3 PR 描述（强制）

每个 PR 的描述写清楚三件事：

1. **目的** —— 这个 PR 解决了什么问题、完成了什么效果
2. **改动** —— 改了什么文件、做了什么（列表形式，一目了然）
3. **测试** —— 改完之后测了什么、效果如何（也是列表形式；开发期未完成就如实写现状）

模板（提 PR 时自动加载）：

```markdown
## 目的

...

## 改动

- ...

## 测试

- ...
```

## C4 提 PR 前自查（强制）

自查清单随 PR 模板自动加载，逐条勾选：

1. 没有残留的调试代码、多余注释或文件
2. 没删掉不该删的原有代码或注释
3. 新功能或修 Bug 不影响已有功能
4. 没有硬编码的临时数值
5. 自己测过，没有新增报错

## C5 评审与合并（强制）

1. 提 PR 时在 GitHub **Reviewers 中 assign 至少 2 名评审人**（优先项目负责人，管理员亦可）——不要只在正文打 @；
2. 至少 1 名评审人批准方可合并；
3. 批准后由项目负责人执行合并；
4. 评审拒绝时**直接引用条款编号**（如「违反 B3」「见 C4-2」），不另行撰写拒绝理由，成员自行查阅对应条款；对条款有疑问可在 PR 内追问；
5. 评审基调：**规范了即可，不苛求完美**——评审只核对上述条款的符合性，不对实现方式、代码风格、功能多少额外挑刺；规范未覆盖的问题可以建议，但不作为拒绝依据。软建议：单个 PR 改动尽量小（建议 300 行以内），便于评审。
